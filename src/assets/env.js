/* eslint-disable @typescript-eslint/no-invalid-this */
(function (window) {
    window.env = window.env || {};

    // Environment variables to be used in environment.ts
    window.env["production"] = false;
})(this);
